OLLAMA_HOST=http://localhost:11434
EMBED_MODEL=nomic-embed-text
VLM_MODEL=gemma3:4b
LLM_MODEL=llama3.2:1b

# Paths are relative to the backend/ folder by default (see app/core/config.py);
# only override these if you moved the data/ folder elsewhere.
# VECTOR_STORE_PATH=data/vector_store
# CHUNKS_PATH=data/all_chunks_embedded.json
# IMAGES_DIR=data/images

TOP_K=4
RRF_K=60

# JSON array of allowed frontend origins
CORS_ORIGINS=["http://localhost:7860", "http://127.0.0.1:7860"]

LOG_LEVEL=INFO
